#pragma once
#ifndef XT
#define XT

#include <iostream>
#include <iomanip>
#include <windows.h>
#include <time.h>
#include <conio.h>
using namespace std;

#define size 10

void byChance(int arr[]);
int* PrintArr(int arr[], int& count, int* NEWarr);
int Sum(int NEWarr[], int& count);
#endif
